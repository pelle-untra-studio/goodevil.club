// @goodevil/ui — shared design system.
//
// The design tokens + base styles live in tokens.css. Import them once at your
// app entry point (e.g. apps/simulator/src/main.tsx):
//
//   import "@goodevil/ui/tokens.css";
//
// Shared React components (card, side panel, nav pill) will be re-exported from
// here over time as the same patterns repeat across probes.

export const TOKENS_CSS = "@goodevil/ui/tokens.css";
